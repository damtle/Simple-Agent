# 贡献指南

感谢你愿意帮助改进 Simple Agent。

## 可以参与的内容

- 报告错别字、失效链接和表述歧义；
- 修正不能运行的示例或测试；
- 补充能够帮助初学者理解 Agent 构造过程的说明；
- 提出目录、导航、无障碍访问和阅读体验方面的改进。

## 提交方式

小问题可以直接创建 GitHub Issue。需要修改文件时，请创建独立分支并提交 Pull Request，在说明中写清修改原因、涉及章节和验证方式。代码变更应通过现有测试，文档变更应确认 Docsify 链接能够正常打开。

参与贡献即表示你同意：提交的软件源代码与代码示例采用 MIT License，提交的书稿正文、图表和非代码文档采用 CC BY-NC-SA 4.0。

项目会通过 Git 历史和 GitHub Contributors 记录实际贡献者。

## 本地验证

在项目根目录的虚拟环境运行 `python -m pytest` 和 `python scripts/check_docs.py`。第十四章快照使用独立虚拟环境，在 `code/chapter14` 内运行它自己的测试，避免两个同名包互相覆盖。仓库的自动检查分别运行两套测试，并检查本地文档链接、围栏和标题层级。

运行轨迹建议保存到 `artifacts/traces/`，该目录不进入版本控制。分享轨迹、截图或错误输出前，应先移除密钥、个人信息及私人任务内容。
